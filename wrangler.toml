# Cloudflare Pages — optional local preview: npx wrangler pages dev .
# Dashboard: connect GitHub repo; build output = repository root (no build command).
name = "faith-driven-life"
compatibility_date = "2024-11-01"
pages_build_output_dir = "."
